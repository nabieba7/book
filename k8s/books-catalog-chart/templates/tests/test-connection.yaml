apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "books-catalog-chart.fullname" . }}-test-connection"
  labels:
    {{- include "books-catalog-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "books-catalog-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
